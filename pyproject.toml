[project]
name = "tabelog-fetch"
version = "0.1.0"
description = "Simple CLI to fetch Tabelog restaurant reviews"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
    "beautifulsoup4>=4.12.0",
    "lxml>=5.0.0",
]

[project.scripts]
tabelog-fetch = "tabelog_fetch:main"
